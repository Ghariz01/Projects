#include <stdio.h>
int main()
{
    float a , b , p , e;
    printf( "Dwse pleures se ekatosta" );
    scanf( "%f%f" , &a , &b );
    p=2*(a+b);
    e=a*b;
    printf( "H perimetros twn pleurwn einai %.2f ekatosta kai to emvadon einai %.2f" , p , e );
    return 0;
}
