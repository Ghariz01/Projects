#include <stdio.h>
#include <math.h>

int main()

{
    int a;
    printf( "Dwse arithmo" );
    scanf( "%d" , &a );

    if( a%2==0 )
        printf( "Zygos" );

    else
        printf( "Monos" );

    return 0;
}
