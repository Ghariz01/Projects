#include <stdio.h>

int main()
{
    int a , b , c , max;
printf( "Dwse 3 arithmous" );
scanf( "%d%d%d" , &a , &b , &c );
max=a;
if ( b>max )
    max=b;
if ( c>max );
    max=c;

    printf( "O megalyteros arithmos einai o %d" , max );
    return 0;
}
